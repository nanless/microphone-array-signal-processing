# agents.md — 对你的长期要求（长期有效，每次更新文档前必读）

> 本文是本仓库对 AI 助手的要求说明书。你以后会被经常叫来更新文档，
> 每次动手前先通读本文件，并严格按下面的标准执行。

## 1. 这是什么项目

一套写给初学者和入门研究生的麦克风阵列信号处理中文教程：从"为什么摆一群麦克风"
讲到定位（DOA）、波束形成、回声消除（AEC）、去混响（WPE）、语音分离、声源追踪，
一直到工程落地与选型。文字平实，公式推导与手算例子给全，33 张图全部可复现。

| 目录/文件 | 说明 |
|---|---|
| `chapters/` | 教程正文 14 篇 Markdown（`00_overview.md` 是入口，`01`～`11` 是 11 章正文，`12`/`13` 是附录 A/B） |
| `figures/` | 33 张插图（`fig01`～`fig33_*.png`），全部由脚本生成、可复现 |
| `scripts/` | 绘图与构建脚本（`make_figures.py`、`make_aec_figures.py`、`build_site.py`、`build_pdf.py`，说明见 `scripts/README.md`） |
| `site/` | 多级页面站（`index.html` 首页 + 13 篇正文页，构建产物，可再生） |
| `dist/` | 合订 PDF（`microphone-array-tutorial.pdf`）与合订 HTML 中间产物 |

构建链（在仓库根目录执行）：

```bash
.venv/bin/python scripts/make_figures.py      # 生成图 1～25、图 33 → figures/
.venv/bin/python scripts/make_aec_figures.py  # 生成图 26～32 → figures/
.venv/bin/python scripts/build_site.py        # chapters/ → site/*.html
.venv/bin/python scripts/build_pdf.py         # 合订 HTML → PDF（需本机 Chrome）
```

## 2. 文档更新的总原则

1. **以后会经常让你更新文档**，把每一次更新都当作正式出版：宁可改得慢，不可改得糙。
2. **所有知识必须好好详细搜索、深度研究。** 无论是概念、例子还是计算，都要讲得很细：
   概念要给定义、给直觉、给适用边界；例子要给完整数字、给出手算过程、给出结论；
   计算要把每一步写出来，关键的近似和忽略项要明说。
3. **先搞懂，再下笔。** 遇到不确定的公式、数字、结论，先查教程内其他章节、
   再查经典教材论文，宁可标注"待核实"也不许编造。仿真数字必须注明实现口径，
   跨实验数字只看趋势、不直接比大小。
4. **改完必须可验证**：改了文字要通读上下文；改了图要重跑脚本看效果；
   改了站和 PDF 要重新构建并打开检查。

## 3. 缺陷巡检：每次都要查这些维度

每次更新文档时，都要把下面整张清单过一遍，任何一条都不能跳过：

- [ ] **缺内容**：有没有该讲却没讲的概念、方法、对比、选型建议？章节之间有没有断链
      （比如正文引用了某节、某图、某公式，实际不存在）？
- [ ] **讲得不好**：有没有逻辑跳跃、因果倒置、先用后定义、符号前后不一致的地方？
- [ ] **讲得太粗略**：一句话带过的关键步骤、没有展开的"显然""易得"、
      缺少数字例子的抽象论述，全部要展开。
- [ ] **讲得太深奥**：超出初学者和入门研究生水平的跳步推导、未解释的术语、
      未铺垫的数学工具，要么加铺垫，要么降到直觉+例子层面先讲。
- [ ] **图画得不好**：坐标轴无标签、无单位、图例不清、配色难辨、字号过小、
      图与正文对不上（图号、曲线、结论不一致）？
- [ ] **例子不够**：每个核心概念至少配一个具体例子或手算；每个关键结论配一张图或一组数字。
- [ ] **写错了**：公式推导、符号、单位换算（dB 用 10log 还是 20log）、
      引用的条件与出处、代码与命令，逐项核对。
- [ ] **结构与导航**：章节标题层级、目录、上一篇/下一篇、站内侧边栏、PDF 书签是否正确？
- [ ] **渲染与排版**：Markdown、HTML、PDF 三端的公式（MathJax）、图片路径、
      表格、代码块、中文标点，有没有乱码、溢出、错位、缺图？
- [ ] **文风问题**：黑话、口癖、AI 腔（见第 6 节），发现一处改一处。

## 4. 工作方法：多 subagent 先找问题，再多 subagent 改

文档更新一律用多 subagent 并行，不许单线程闷头改：

1. **找问题阶段**：派多个 subagent 分头做"超级详细理解 + 缺陷 hunting"，
   建议切分（按实际情况增减）：
   - 教程体系：14 篇通读，查结构、断链、难度曲线、学习路径；
   - 核心算法：DOA / 波束 / AEC / WPE / 分离 / 追踪，查推导、例子、数字；
   - 基础与几何：问题定义 / 信号模型 / 阵列形态 / 附录 A，查直觉与承接；
   - 工程链：`scripts/` + 33 张图 + `site/` + PDF，查可复现性与构建；
   - 落地与选型：第 10/11 章 + 附录 B，查场景、指标、练习、排错。
   每个 subagent 返回结构化缺陷清单（文件+行号/图号+问题描述+严重程度+修改建议）。
2. **去重与排期**：你负责把各路清单合并去重，按"写错 > 缺内容 > 讲得不好 >
   太粗/太深 > 缺例子 > 图不好 > 渲染排版 > 文风"排序。
3. **改进阶段**：再派多个 subagent 分头改，每人只认领互不重叠的章节或图纸，
   要求给出改前改后对照。改完由你统一通读，保证术语、符号、图号、结论全书一致。
4. **验收**：重跑绘图脚本、重建站点、重建 PDF（或按改动范围部分重建），
   打开成品逐项核对缺陷清单是否清零。

## 5. 质量标准：四句话

1. **结构清晰**：每篇开头有导航、每节按"直觉 → 公式（每个符号都解释）→
   实测效果 → 工程选型"的固定顺序讲，节与节之间有承接句。
2. **图表精美、信息全面详实**：图要坐标、单位、图例齐全，风格与全书统一；
   表要条件齐备；关键数字一律标注条件与出处。
3. **文字平实，适合初学者和入门研究生**：从生活例子讲起，先会看图再啃公式；
   公式编号手写（如 `(4-1)`、`式(5-1)`），引用写"见式(5-1)"；
   缩写首次出现给全称。
4. **公式推演和例子讲解要详细**：不跳步、不"显然"，手算例子给全中间数字和结论，
   并说明这个例子证明了什么、边界在哪里。

## 6. 文风红线：写人话，不写 AI 腔

- **模仿人类写法**：短句为主，一句只讲一件事；用"你"和"我们"的口吻带读者走；
  该举例时举例，该停顿时分段。
- **不要有任何黑话和口癖**：不许用圈内黑话、不许用网络口癖、不许用故作深刻的词藻；
  术语第一次出现必须给中文解释，之后全书统一。
- **不要写诡异的 AI agent 文法**，例如：
  - ❌ "首先……其次……最后……总之……"式的八股排比；
  - ❌ "值得注意的是""不可否认的是""发挥重要作用"这类空话套话；
  - ❌ 中英夹杂乱用、标点中英混用、长到读不完的套娃从句；
  - ❌ 把简单事说复杂、把一句话能讲清的事铺成三段。
- 好句子的样子（照着这个感觉写）：
  - ✅ "麦克风数量翻倍，信噪比能提高多少分贝？这笔账要分三种情况算。"
  - ✅ "时延估计要做到比采样间隔更细，所以要做插值。"

## 7. 范围：不只是 md

- `chapters/*.md`、`README.md`、`README_EN.md`、`scripts/README.md` 的**所有文字**；
- `figures/` 的**所有图像**（内容正确性 + 美观程度 + 与正文的一致性）；
- `site/*.html`、`dist/combined.html`、`dist/microphone-array-tutorial.pdf` 的
  **文字、图像、乃至渲染**（公式渲染、图片引用、导航跳转、目录书签、表格排版）。
- 发现渲染问题时，多 subagent 详细找出来（具体到文件、浏览器/PDF 阅读器表现、
  复现步骤），再多 subagent 详细改正并重新构建验证。

## 8. 工程纪律

- 用 `read` 看文件、用 `edit` 做针对性修改、用 `write` 建新文件；先读后改，
  不要在没读过原文的情况下改写。
- 改图只改 `scripts/make_figures.py` / `make_aec_figures.py` 里对应的 `fig_*()` 函数，
  不许直接往 `figures/` 里丢手画的图（保证可复现，随机种子固定）。
- 大的改动先列提纲再动手；一次改动只做一件事，改完即验证。
- 联网研究用 Tavily MCP（工具名形如 `mcp__tavily__tavily_search`，已配进 web profile；若某会话里没有该工具，跑 `bash ~/tools/tavily-search.sh "查询词" [条数] [--answer]`，key 已内置）。子智能体没有联网能力，需要核实就由你亲自查，查完把结论喂给它们。
- 完工时在回复里列出主要产出文件的路径（用 Markdown 行内代码格式），
  并用 `present` 标记用户要接收的交付文件。

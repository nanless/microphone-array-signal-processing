# AGENTS.md — 本仓库的长期协作规范

> 每次修改本仓库前先通读本文件。本文件既规定内容质量，也规定调查、修改、验证和交付方式。
> 规则发生冲突时，按第 2.1 节的优先级处理，不要机械执行低优先级条款。

## 1. 项目定位与文件地图

### 1.1 教程面向谁

本项目是一套面向初学者和入门研究生的麦克风阵列信号处理中文教程。内容从阵列为何有效讲起，
逐步进入声源定位（DOA）、波束形成、声学回声消除（AEC）、去混响（WPE）、语音分离、
声源追踪、工程实现与选型。

正文应让读者同时得到四样东西：

1. 概念的准确定义和直觉；
2. 不跳关键步骤的公式推导；
3. 可以复算的数字例子；
4. 方法的适用条件、失效边界和工程选择依据。

读者不需要预先熟悉阵列处理，但可以具备基础的三角函数、复数和线性代数知识。较长的数学预备
内容放入附录，正文只提供当前推导所需的最小铺垫并链接过去。

### 1.2 源文件与生成物

修改前先判断文件属于源文件还是生成物。生成物出现问题时，要修改对应源文件或构建脚本后重新生成，
不能只修成品。

| 类型 | 路径 | 作用 | 修改规则 |
|---|---|---|---|
| 教程源文件 | `chapters/*.md` | 导读、11 章正文和 2 篇附录 | 直接修改；改后检查跨章引用 |
| 教学代码源文件 | `codes/array_tutorial/*.py`、`codes/examples/*.py` | 与公式对应的最小实现和可运行例子 | 直接修改；必须同步测试、算法覆盖表和对应章节 |
| 音频生成源与生成物 | `codes/array_tutorial/audio_samples.py`、`codes/examples/generate_audio_samples.py` → `codes/audio/*.wav`、`MANIFEST.json` | 14 组、60 个数学合成音频样本及可核验参数 | 修改源代码后重新生成；禁止手改单个 WAV 或把合成数据称为真实录音 |
| 房间仿真源与生成物 | `codes/examples/room_srp_exercise.py` → `codes/room_audio/` | 6 个源位各有源、仅直达和完整房间输出，共 18 个白噪声合成 WAV、独立清单与一张结果图 | 使用隔离的 pyroomacoustics 0.10.0 环境生成到新目录并核验后更新；不得混入主清单的 60 个样本或称作真实录音 |
| GSS 教学链源与生成物 | `codes/array_tutorial/gss_teaching.py`、`codes/examples/gss_teaching_demo.py` → `codes/gss_audio/` | 5 个数学合成 WAV、`STATE.npz` 和独立清单 | 重生后核对共同增益、状态摘要与评分；不把教学子链称为已运行官方 GPU/CHiME 整链 |
| 移动声源源与生成物 | `codes/array_tutorial/moving_source.py`、`codes/examples/moving_source_audio.py` → `codes/moving_audio/` | 3 个自由场数学合成 WAV 与轨迹真值清单 | 以传播时延和距离衰减的源模型生成；不与主 60 个 WAV 混算或称为真实录音 |
| 真实录音与派生文件 | `codes/array_tutorial/real_recordings.py`、`codes/examples/prepare_real_recordings.py` → `codes/real_audio/` | DEMAND 真实同步录音摘录和派生文件，共 4 个 WAV | 原始归档固定摘要；截取范围、通道次序、变更和独立数据许可随资产保留；不与合成清单混用 |
| 外部代码索引 | `codes/SOURCES.lock.json`、`codes/THIRD_PARTY.md` | 官方仓库、精确版本、许可证和使用边界 | 只记录已核实来源；不把链接或源码可见误写成可自由再分发 |
| 源码状态生成物 | `codes/SOURCE_STATUS.json` | 当前锁定清单对应的本地获取与范围核对结果 | 由获取工具 `--verify --report` 生成，不手工改成成功；方法级运行另记 |
| 源码研究文档 | `codes/research/*.md` | 逐算法源码入口、实现差异、工业配置与复现实验 | 与正文和覆盖表互链；区分建议实验和已执行结果 |
| 项目说明源文件 | `README.md`、`README_EN.md`、`scripts/README.md` | 项目入口、英文说明、构建说明 | 直接修改；中英文共有信息要同步 |
| 绘图源文件 | `scripts/make_figures.py`、`scripts/make_aec_figures.py` | 生成全部插图 | 图有问题时修改这里，不手工修 PNG |
| 构建源文件 | `scripts/build_site.py`、`scripts/build_pdf.py`、`scripts/heading_aliases.py`、`scripts/legacy_sequential_anchors.json` | 生成站点、合订 HTML 和 PDF，并保护已发布深链的原语义 | 页面结构或渲染有问题时修改这里；标题改号后逐项校验历史语义锚 |
| PDF 公式第三方资源 | `scripts/vendor/mathjax-3.2.2/` | 固定版本的 MathJax 脚本、按需扩展和字体 | 保留上游许可及来源摘要；更新版本时同步更新构建核验与渲染抽查，不手改压缩脚本或字体 |
| 插图生成物 | `figures/fig*.png` | 正文插图 | 由绘图脚本生成，不直接编辑 |
| 站点生成物 | `site/*.html`、`site/research/*.html`、`site/room_audio/`、`site/gss_audio/`、`site/moving_audio/` | 14 篇教程、6 篇研究手册页面与独立实验媒体副本 | 由 `build_site.py` 生成，不直接编辑 |
| 合订生成物 | `dist/combined.html`、`dist/microphone-array-tutorial.pdf` | 合订 HTML 和 PDF | 由 `build_pdf.py` 生成，不直接编辑 |

`chapters/` 当前包含 14 篇 Markdown，研究手册另有 6 篇网页源文件，项目当前有 39 张编号图和附录 B 房间题的一张补充结果图。文件数量、图号、章节名称或构建入口发生变化时，要同步
更新本节、README、导读中的文件地图和构建说明。

### 1.3 构建入口

在仓库根目录执行：

```bash
.venv/bin/python codes/examples/generate_audio_samples.py  # 合成 60 个 WAV 与清单
.venv/bin/python -m codes.examples.gss_teaching_demo  # 独立 GSS 教学音频与状态
.venv/bin/python -m codes.examples.moving_source_audio  # 独立连续移动双麦音频
.venv/bin/python codes/examples/prepare_real_recordings.py --check  # 离线检查 4 个真实录音/派生 WAV
.venv/bin/python scripts/make_figures.py      # 图 1～25、图 33～36
.venv/bin/python scripts/make_aec_figures.py  # 图 26～32、图 37～39
.venv/bin/python scripts/build_site.py        # chapters/ → site/*.html
.venv/bin/python scripts/build_pdf.py         # chapters/ → 合订 HTML 和 PDF
.venv/bin/python -m unittest discover -s tests -p 'test_codes*.py' -v  # 教学代码回归
```

只改文字时不必重画图片。改了绘图脚本，要生成并检查相关图片；改了正文、导航或构建脚本，要重新
生成受影响的 HTML 和 PDF。PDF 公式依赖仓库内 MathJax 资源，无需联网；站点公式目前仍需联网。
若本机缺少 Chrome、依赖、必要的联网条件或权限，应记录未完成的验证及原因，
不能声称已经验收。

## 2. 规则优先级、授权与范围控制

### 2.1 规则优先级

下面三项是必须同时满足的硬条件：

- **遵守上级规则和数据安全边界**：不执行越权或高风险操作，不破坏用户数据和历史。
- **遵守用户当前任务和授权边界**：授权决定可以做什么，但不允许编造事实、公式、数字或出处。
- **保证事实正确**：不能为了完成任务、补齐数字或迎合措辞而写入已知错误或无依据的断言。

在三项硬条件都满足后，按下面顺序权衡质量目标：

1. **来源可靠与结果可复现**：关键结论写清条件和出处；仿真写清配置、实现口径和随机性。
2. **读者能理解**：定义、直觉、公式、例子和边界形成完整解释，但不把旁支知识无限展开。
3. **全书一致与生成物同步**：术语、符号、图号、导航和构建产物保持一致。
4. **文风与版式**：语言自然、图表清楚。文风优化不能改变技术含义，也不能代替事实核查。

低优先级规则不能推翻高优先级规则。为了补一个“具体数字”不能编造实验结果；为了统一措辞，不能
把有明确含义的专业术语换成不准确的日常说法。

### 2.2 授权边界

- 阅读仓库和运行只读检查不改变项目状态。用户要求修改时，运行项目已有的本地构建和检查属于正常
  验证步骤；纯审查任务不因“验证”而改写生成物。
- 修改范围以用户任务为准。发现范围外问题时，记录文件、行号和影响，不顺手重写无关章节。
- 不主动创建外部问题、发送消息、发布站点、提交或推送代码，除非用户明确要求。
- 工作区可能已有用户或其他协作者的修改。开始前查看差异；遇到重叠修改时先协调，不覆盖、不回退。
- 未决问题先写入本地审查记录。只有用户明确要求时，才创建外部问题或工单。

### 2.3 不确定性如何处理

审查清单和工作草稿可以标“待核实”，以避免猜测。正式交付的 Markdown、HTML 和 PDF 不保留
“待补”“链接待补”“数字待查”等编辑占位。

若证据本身不足，应改写为有来源的审慎结论，明确目前能确定什么、不能确定什么。若该事实不是当前
内容成立所必需，也可以删去具体断言。不能用删除问题记录、降低检查标准或编造数字来制造“已经完成”。

### 2.4 控制改动范围

动手前写清本次修改的目标、涉及的源文件和验收方法。一次提交围绕一个明确主题；同一主题可以修改
多个相互依赖的文件，但不要夹带无关清理。

术语或公式发生变化时，检查它的直接消费者：定义处、引用处、符号表、图注、导读和练习答案。
纯排版或错字修复不自动升级成全章重写。发现更大问题时先登记，再决定是否纳入当前任务。

新增、删除或移动带编号的练习、图、表和公式后，按引用对象的题意或内容逐项核对所有交叉引用。
不能假定后续编号只需统一加减，也不能只检查链接是否存在；引用必须仍然指向原本要推荐或解释的内容。

已发布的 `sec-x-y` 和 `sec-N` 深链也属于引用契约。调整标题前先记录旧锚对应的主题，避免把旧号
重新用于不同主题；改号后在站点与合订本同时保留指向原主题的历史别名。已有别名不代表正文显示的旧
节号可以保留，跨章文字和研究手册仍需按新结构逐条改写。目录层级由独立主题决定，不为了维持旧书签
总数压低合理标题。

## 3. 分级工作流程与多智能体协作

### 3.1 先按风险和影响范围分级

改动行数不是唯一标准。一行公式修正可能影响全书，不能按小修处理。

| 级别 | 适用情况 | 典型例子 |
|---|---|---|
| **小修** | 局部措辞、错字、格式或明确的失效链接；不改变技术结论、公式、图或章节结构 | 改错别字、统一标点、修正本地链接 |
| **章节级** | 改变一个或数个相关章节的技术内容、公式、例子、图、术语或学习顺序 | 修正 HPBW 推导、补完整算例、重画相关插图 |
| **全书级** | 改变全书结构、统一符号和术语、批量清理文风、调整构建链或系统审校全部内容 | 重排章节、全书公式审校、重做站点导航 |

无法判断时按较高一级处理。涉及核心公式、单位、来源、实验数字或生成脚本的修改，至少按章节级处理。

### 3.2 小修流程

小修可以由主智能体独立完成：

1. 阅读目标位置及其上下文，确认没有同名符号、重复定义或引用受影响；
2. 做最小修改，不顺手改写无关段落；
3. 检查差异、链接和 Markdown 结构；
4. 若修改进入站点或 PDF，重建受影响的生成物并抽查对应页面；
5. 说明修改位置和实际完成的验证。

检查中发现技术结论也要改变时，立即升级为章节级流程。

### 3.3 章节级流程

章节级更新分为“找问题、确定方案、修改、验收”四步：

1. **找问题**：通读目标章节及直接引用它的段落。协作能力可用时，至少安排一路独立只读审查，
   从公式、教学结构或工程复现中的另一个角度复核。
2. **确定方案**：合并重复问题，确认当前范围；核心公式和外部数字先核实，再修改。
3. **修改**：按文件划分写入责任。同一文件同一时段只由一个智能体修改；共享的导读、符号表、
   README 和构建脚本由主智能体统一处理。
4. **验收**：重读上下文，核对跨章引用；重跑相关绘图脚本；重建站点和 PDF；检查对应的图片、
   公式、表格、导航与书签。

若当前环境没有多智能体能力，主智能体应分两轮检查：第一轮查技术正确性，第二轮查跨章一致性和
渲染结果。不能把这两轮自检说成独立并行复核。

### 3.4 全书级流程

全书级更新必须先完成只读审查，再开始写入。协作能力可用时，按互不重叠的主题并行：

- 体系、学习路径与跨章导航；
- 基础、几何、数学附录与符号；
- DOA、波束、AEC、WPE、分离和追踪；
- 工程实践、选型、练习与排错；
- 绘图脚本、构建脚本、站点和 PDF。

只读审查阶段，各智能体只提交缺陷记录，不改文件。主智能体合并、去重并确定优先级后，再进入修改。
修改阶段按互斥文件分工；跨分工区的术语、符号、图号、导航和共享文件由主智能体统一修改。

共享工作区中，每个写入者修改前要重新读取目标文件，确认审查后没有新变化。发现重叠时停止写入并
协调归属，不用覆盖、回退或重做他人的修改来解决冲突。生成物在所有源文件稳定后由一处统一构建。

全书修改完成后，运行全部绘图和构建脚本，抽查所有章节页面，并检查 PDF 的目录、书签、公式、
图片、表格和分页。验收报告应逐项对应缺陷记录，不能只写“构建成功”。

### 3.5 缺陷记录、严重度与状态

每条缺陷使用同一格式：

```text
ID｜文件与行号/图号｜类别｜严重度｜证据｜修改建议｜负责人｜状态｜验证结果
```

严重度定义：

- **P0 阻断**：无法构建、产物严重缺失，或存在数据安全风险。修复前不能宣称产物可发布。
- **P1 严重**：核心公式、数字、单位、引用或结论错误，会直接教错读者。正式发布前必须修复。
- **P2 重要**：逻辑跳步、适用边界缺失、例子或导航明显不足，影响理解或复现。
- **P3 一般**：局部排版、措辞或轻微不一致，不改变技术结论。

状态只使用以下几种：

- **已发现**：已经记录，尚未决定是否纳入当前任务；
- **本次处理**：已确认在当前范围内，尚未完成；
- **已修改**：源文件已经修改，等待验证；
- **已验证**：修改和受影响产物均已检查；
- **延期**：当前无法处理，已写明原因、所需证据和后续动作；
- **范围外**：与本次目标无直接关系，保留记录但不修改。

验收不要求把所有问题强行改成“已验证”，但要求每条都有真实状态。P0、P1 不得以“延期”状态进入
正式发布；确实无法解决时，应停止发布并向用户说明。P2、P3 可以在说明理由后延期，不能删除记录来
制造“问题已经清零”的假象。

### 3.6 审查维度

审查只覆盖当前任务及其直接影响范围；全书级任务才逐项覆盖全部章节和产物。每次确定审查范围后，
从下面这些维度检查：

- **内容完整性**：学习目标需要的概念、方法、对比和选型依据是否齐全，跨章引用是否存在。
- **逻辑与顺序**：是否先用后定义、因果倒置、符号换义，章节之间是否缺少必要铺垫。
- **讲解深度**：关键步骤是否过粗，背景和旁支是否展开过度，难度是否符合目标读者。
- **公式与数字**：推导、单位、近似、指标口径、来源和实现条件是否正确。
- **例子与边界**：核心概念是否有可复核例子，结论是否说明适用条件和失败情形。
- **图表**：坐标、单位、图例、配色、字号、图注及正文结论是否一致。
- **结构与导航**：标题层级、目录、上一篇/下一篇、侧边栏、公式与图表引用、PDF 书签是否正确。
- **渲染与可复现性**：Markdown、HTML、PDF 的公式、图片、表格和代码块是否正常，脚本能否复现产物。
- **可访问性**：网页能否只用键盘访问、焦点是否可见、当前页与按钮是否有语义标签、表头是否正确；
  PDF 是否保留可搜索文本、语言、阅读顺序和书签。构建链暂时不能提供的标记能力要在交付中明说。
- **文风**：是否存在第 6 节列出的模板腔、黑话、口癖、绝对化断言和编辑痕迹。

## 4. 证据、引用、时效性和数字口径

教程中的公式、数字和工程结论都要能追到依据。搜索只负责找到资料，搜索摘要不能充当证据。
写作前先判断内容需要哪一级证据，写完再检查适用条件是否齐全。

### 4.1 证据等级

| 等级 | 来源 | 可以支持什么 | 使用要求 |
|---|---|---|---|
| **A：原始依据** | 标准原文、官方规范、原始论文、官方数据集说明、官方源码和版本文档 | 公式来源、标准条款、算法定义、性能数字、软件行为 | 优先使用；尽量标出章节、公式号、表号、版本或提交号 |
| **B：权威解释** | 经典教材、同行评议综述、大学课程资料 | 共识性定义、历史脉络、直觉解释、方法分类 | 可用于讲解，不能代替原始性能数字；教材注明版本，页码稳定时一并给出 |
| **C：项目或厂商材料** | 项目 README、厂商技术文档、产品页面、作者博客 | 该实现的参数、接口、产品声明和复现步骤 | 只能证明该项目或厂商声称的内容；注明版本和核实日期，不外推为行业共识 |
| **D：发现线索** | 搜索摘要、聚合页、论坛、转载、未核实的二手解读 | 帮助找到关键词和原始来源 | 不得支持正式结论，也不列入正式参考资料；只能用于定位 A～C 级来源 |

同一结论有多个来源时，选最接近原始事实的来源。不能用大量低等级链接掩盖缺少原始证据。
综述引用某项性能数字时，仍要回到产生该数字的原始论文或官方榜单核对。

### 4.2 哪些内容必须引用

- 首次引入并非本书自行推导的经典公式、算法或命名时，给出原始论文、标准或教材出处。
- 引用论文结果时，标明具体表格、图或实验节；不能只链接论文首页让读者自行寻找。
- 标准限值、认证条件、数据集规模、产品规格、软件默认值、复杂度实测和性能数字必须有来源。
- “首次提出”“当前最好”“主流方案”“工业默认”“截至目前”等范围很大的判断，必须核实范围和日期；
  没有充分证据就缩小表述。
- 本仓库自行推导、手算或仿真的结果要明确写“本书推导”或“本书仿真”，并指向脚本或实现位置。
- 公认的基础代数步骤不必逐行引用，但起始模型、特殊假设和非通用近似仍要注明来源或推导。

优先使用 DOI、标准组织页面、论文正式页面、官方仓库等稳定链接。引用网页时记录标题、机构、版本和
核实日期。链接失效不能成为结论无法追溯的理由，正文还要写清来源名称和关键定位信息。

### 4.3 时效性

软件 API、依赖版本、排行榜、论文发表状态、标准版本、产品规格、许可证、数据集下载方式和项目维护
状态容易变化。每次改到相关段落时都要重新核实。

- 预印本必须标成“预印本”，不能写成已经同行评议或正式发表。
- 已接收但未正式出版的论文要写清状态和会议年份，不能猜测卷期、页码或最终版本。
- “最新”“目前”“近年”必须带核实日期或明确时间范围。时间不是结论重点时，直接写论文年份。
- 同名方法存在多个版本时，性能数字、参数量和实时性必须绑定到具体版本。
- 挑战赛信息必须绑定届次、任务或赛道、开发集或评测集、结果页面状态和核实日期。任务尚未开放、
  正在提交、已经结束和已经公布结果是不同状态；不能把赛前任务说明当作最终榜单，也不能用旧届规则
  解释新届结果。
- 找不到可靠来源时，应补证据、降低断言强度或删除；不得把“待核实”留在出版正文中。

新增近年算法前再过一道收录门槛：正文必须能说明它解决了哪个已出现的问题、相对基线改变了哪一步、
依赖哪些假设，以及读者怎样复现一个最小例子或用它作选型。四项中缺少两项时，不把论文名或模型名
加入正文目录。删除错误、无来源、重复或偏离学习目标的材料时，要同步检查引用和练习，并在交付报告
中说明删除理由。

### 4.4 性能数字的最小口径

性能数字只有在条件可还原时才有意义。引用或制作结果表时，按任务补齐下列信息：

1. **数据**：数据集、版本、训练/验证/测试划分，以及真实录音还是合成数据。
2. **声学条件**：采样率、通道数、阵列几何、声源数量、SNR、SER、混响时间和噪声类型；
   不适用的项可以省略。

    数据集或设备含多种录音来源时，还要分开写设备数量、每台设备的物理通道数、算法实际选用通道、
    可穿戴或近讲参考通道，以及这些参考用于转写、同步、训练还是评分。不能把“6 台四麦设备”缩写成
    “6 麦阵列”，也不能把辅助转写或同步的参考录音写成干净评分参考。

3. **算法条件**：窗长、帧移、滤波器长度、保护延迟、迭代次数、是否因果、是否使用未来帧和额外先验。
4. **指标**：指标全名、单位、方向和具体变体。例如 SI-SDR 与 SI-SDRi、绝对 WER 与相对 WER
   降幅不能混写。
5. **基线**：与谁比较，基线是否使用同一数据、预处理、采样率和打分脚本。
6. **统计方式**：均值、中位数、单个样例还是区间；多次随机实验要给次数和离散程度。
7. **资源条件**：声称实时性、延迟、内存或速度时，写硬件、线程数、批量大小、模型版本和计时范围。
8. **来源定位**：论文表号、图号、标准条款或本仓库脚本路径。

条件不同的实验不能直接按数字排高低。确实需要并列时，只讨论共同趋势，并在表头或正文标出
“条件不同，不可横向比较”。绝对提升、相对提升和百分点必须分开。例如 WER 从 20% 降到 15%，
是下降 5 个百分点，或相对下降 25%，不能写成“提升 5%”。

本书仿真还要记录输入信号、随机种子、归一化方式、参数表和生成脚本。显示数字的有效位数不得超过
实验分辨率；正文、图、表和脚本采用同一舍入规则。

重复抽样实验中，离散分类结果要保留各类别计数；连续指标要保留逐次值或足以复现统计量的生成步骤。
两者都要报告失败次数、统计分母和区间计算方法，说明各次是否独立、各条件是否共享随机输入。
固定一组特征值再改变快拍数只属于参数敏感性分析，不是重新采样实验；抽样比例的
置信区间也不等于某一次算法输出的置信度。条件比较需说明同时改变了哪些因素。

比较输出与参考信号时，还要写清固定传播时延、算法延迟和增益是否已经对齐或补偿。若指标故意保留
这些差异，名称和解释必须明确，不能把由时移或整体增益主导的误差解释成谱形、失真或算法优劣。
比较多个软件接口时，还要逐行标明线性残差、残余抑制后输出或其他实际取点，以及参考和采集的调用
顺序。使用同一输入不意味着内部控制、取点或固定输出延迟相同；无法取得同级输出时只陈述各自观察，
不把总输出功率比称为干净回声分量指标。

多目标追踪实验要分开报告无标签的位置集合误差、轨迹身份错配和缺测时的状态不确定度；
集合位置正确不能证明说话人身份正确。使用真值标签进行关联只能作为诊断对照，不能作为可部署算法的
结果。涉及波束跟随时还要给出观测、滤波、控制的时间间隔与限速，并量化方向滞后。

## 5. 公式、例子、图表、术语与初学者质量标准

核心算法章节整体应覆盖“问题与直觉、数学模型、可复核例子、实测或仿真证据、适用边界、工程选型”，
但不强迫每一节重复同一模板。历史、符号表、故障排查、练习和参考资料按内容需要组织。

### 5.1 公式核查协议

新增或修改公式时，按下面顺序检查：

1. **先写前提**：说明远场/近场、窄带/宽带、平稳性、独立性、线性、因果性等假设，以及公式
   在哪些条件下成立。
2. **再定约定**：固定傅里叶变换、时延正负、角度零点、导向矢量相位、内积和坐标系。与前章
   不同时必须明确提醒。
3. **解释符号**：变量首次出现时给出含义、标量/向量/矩阵类型、维度和单位。复数变量要区分
   转置、共轭和共轭转置。
4. **写出关键中间步**：不能用“显然”“易得”跳过会改变符号、维度、归一化或约束的步骤。
   重复的基础代数可以略写，但要告诉读者略去了什么。
5. **检查数学一致性**：核对矩阵维度、求和上下限、下标、正负号、复共轭、归一化常数和量纲。
   概率检查非负性与归一性；功率检查非负性；协方差矩阵检查厄米性与半正定性。
6. **检查边界情况**：代入零噪声、单通道、两个阵元、无混响、完全匹配等简单情形，确认公式能
   退化到已知结果。近似式必须说明误差来源和失效条件。
7. **独立复算**：核心公式至少用一个小规模数字例子复算。正文手算、绘图脚本和实现代码必须采用
   同一约定；不能用脚本生成的图反过来证明脚本本身正确。
8. **编号与引用**：算法定义、后文会再次引用的闭式解、目标函数、状态递推和评价指标定义必须编号；
   只在当前行使用的单位换算或临时代数不编号。编号统一写在公式块内，如 `\tag{5-1}`，正文写
   “见式(5-1)”。同章顺序连续，全书不得重复，所有交叉引用都必须指向实际存在的公式。

涉及 dB 时单独检查口径：

- 功率、能量、方差和功率谱密度之比使用 $10\log_{10}(P_2/P_1)$。
- 当功率与所比较幅度的平方成正比，且两边比例常数相同时，幅度、声压、电压或频谱模之比使用
  $20\log_{10}(|A_2|/|A_1|)$；否则应从功率定义重新推导。
- ERLE、SNR、SER 等必须写清分子、分母和统计时间段；双讲段是否计入要明确。
- dB SPL、dBFS、dBm 等带参考量的单位不能只写成 dB，也不能互相直接比较。
- 报“提高 3 dB”时要说明提高的是功率、幅度、增益还是某个评价指标，不能只凭数值相减推断
  物理意义。

### 5.2 例子分级

不是所有例子都要展开到同样长度。按作用分为四类：

| 类型 | 用途 | 最低要求 |
|---|---|---|
| **锚点手算** | 帮读者第一次走通核心算法 | 给出全部输入、单位、公式代入、关键中间数字、舍入规则、结果和结论；每个核心算法至少一个 |
| **辅助例子** | 解释局部步骤或参数影响 | 只展开与当前问题有关的步骤，但不能省略决定正负号、数量级或结论的中间量 |
| **工程案例** | 说明参数选择、资源代价或故障表现 | 给出设备、数据、声学条件、实现版本、评价方法和适用范围；不能把单台设备结果写成普遍规律 |
| **反例/边界例子** | 推翻过强直觉或展示算法失效 | 明确改变了哪个条件、哪一步失效，以及不能由该反例推出什么 |

锚点例子结尾回答两件事：“这个例子说明了什么？”“结论在哪些条件下不成立？”示意数字要标成
示意；实测数字要能追到数据或记录；仿真数字要能追到脚本。不同输入、指标或数据集的两个例子，
不能用结果大小直接证明参数优劣。

### 5.3 图表的客观标准

所有图都由脚本生成，不能手工修改 PNG 来掩盖脚本问题。随机过程固定种子，图中的曲线、标注和
正文结论必须来自同一组参数。

图的验收项：

- 坐标轴有名称和单位；无量纲量也要写清定义。
- 图例与曲线一一对应，不能只靠颜色区分；同时使用线型、标记或直接标注，灰度打印仍可辨认。
- 按 PDF 和网页的最终显示尺寸检查字号，不得裁切标题、轴标签、图例和注释。
- 缩放到 A4 正文约 165 mm 宽后，承担结论的文字通常不低于约 7 pt；缩放到网页约 860 px 宽后，
  通常不低于约 11 CSS px。达不到时优先拆图、减少注释或改成正文表格，不以原始 PNG 很大为由保留小字。
- 图注写清阵列、采样率、频率、SNR、混响、归一化等理解结论所需的条件。
- 比较同一物理量的子图使用一致的坐标范围、单位和视觉编码；展示不同物理量时分别标清坐标和单位，
  并保持 `(a)(b)(c)` 编号、字体和配色语义一致。正文逐一解释每个子图的读图重点。
- 比较图使用公平的坐标范围和基线。截断坐标轴、对数坐标、平滑或归一化会改变视觉判断时，
  必须标明。
- 随机或统计结果给出实验次数和离散程度；单次示例不能画成普遍性能结论。
- 图脚本先用已知简单输入做检查，再用于正文。关键曲线至少核对一个采样点或极限值。
- 图中展示的系统模型必须与生成数据的有效模型一致。脚本若对脉冲响应、输入或输出额外乘以增益、
  加入固定时延或执行归一化，应把这些操作并入图中的对应量，或在图注中明确写出；不能画一个模型、
  用另一个模型生成曲线。
- 从仿真脉冲响应分离直达与反射分量时，先核查库对每条响应的高通、裁剪、时轴和归一化处理。
  两条响应做差前应采用相同长度和相同处理边界，并用只含直达路径的输入复核；否则处理边缘的差异
  可能被误计为反射能量。
- 系统框图要画出算法成立所必需的信号、参考、状态和控制输入。AEC 不能省略播放参考与声学回路，
  掩码波束不能把说话人活动、掩码、协方差和导向控制合并成一条含义不明的箭头。

表的验收项：

- 每列有名称、单位和统一精度；缺失、不适用和未测量使用不同标记并在表下注明。
- 性能表带齐第 4.4 节的数字口径；条件不同的行不能用粗体暗示统一排名。
- 公式或符号表保持符号、字体和上下标一致；过宽表格要拆分，不能依赖 PDF 横向溢出。
- 表中数字要与正文和脚本复核，正文引用时指出具体行、列或比较对象。

### 5.4 术语、缩写和符号

- 每个可独立阅读的章节中，术语第一次出现时写“中文名（英文全称，缩写）”，后文使用统一中文名
  或缩写。读者可能从网站直接进入某一章，不能只依赖首页定义。
- 一章需要集中引入较多缩写时，可以在章首放一张最小缩写表。表中已经完整展开并解释的缩写，后文
  不必重复长英文；表格不能只列英文全称而不说明本章中的作用。
- API、代码标识、论文正式方法名和没有稳定中译名的术语保留英文，不强造中文译名。
- 同一概念全书只保留一个首选译名；确有常见别名时，在首次出现处说明，后文不交替使用。
- 同一中文或英文词在不同算法中指向不同操作时，应分别给出作用维度、数学算子和适用目的；例如逐频
  互谱幅度归一化与跨通道协方差白化不能共用一个未加限定的定义。
- 同一符号不能在相邻章节无提示地改义。必要时在章首列符号表，并与附录 A 同步。
- 缩写展开不能代替解释。首次写出 MVDR 时，还要说明它最小化什么、约束什么。
- Hz、kHz、ms、dB 等通用单位不要求逐章展开英文全称，但数值与单位之间、大小写和复数形式必须统一。

### 5.5 面向初学者的验收

- 新概念从可观察问题、最小数字例子或准确类比切入，不强求每节都使用生活故事。
- 术语、数学工具和符号先定义再使用。需要线性代数、概率或复数知识时，先给最小铺垫并指向附录。
- 一句话保留一个主要判断；成立条件、单位和必要限定可以与结论同句，不能为了短句把条件拆丢。
- 推导结束后用文字解释结果中各项的物理意义，再说明它怎样影响参数选择和失败模式。
- 类比只帮助建立第一层直觉，随后回到变量、公式、图或可执行步骤。类比可能造成错误推论时，
  明确指出映射边界。
- 章节结尾检查学习目标：读者应能解释问题、复算一个例子、说出适用条件，并在候选方案之间作出
  有依据的选择。
- 长章节的标题层级还要服务于导航。若一个可独立学习的算法、实验或排错主题已经包含多步推导、
  图表和练习，不应仅作为没有目录入口的深层小标题。检查网页目录与 PDF 书签能否让读者直接到达，
  不能为了维持旧的书签数量而压低合理层级。
- 审稿不能只问“读起来顺不顺”。优先让未参与写作的审查者按正文复算一个例子、解释一张图，并
  指出算法何时失效；无人可用时，由主智能体在独立的第二轮完成复算，并在报告中注明缺少独立审查。

### 5.6 段落与块级结构

段落按说明任务划分，不按源码行宽或固定字数划分。一个自然段通常只承担一种主要任务，例如定义、
推导、算例、结果解释、失效边界或工程选择。条件与它限定的结论、公式与第一层符号解释、紧密的因果链
可以留在同段或紧邻的段落中；不要为了追求短句把它们拆散。

- Markdown 中单独换行不会形成新段落。需要在网页和 PDF 中分段时，源文件必须留空行；列表、公式、
  表格、代码块和引用块前后也要保留稳定的空行与缩进。
- 有序列表中的续段和公式使用四空格归属原项；导语与首项、续段与下一顶层项之间留空行。
  修复时在生成 HTML 中检查实际 `<ol>/<li>` 数量和公式所属项，不能只看源码的 `1.`、`2.` 编号。
- 定义、公式前提、公式块和第一层符号解释保持相邻。物理意义、完整数字例子、工程选择和失效边界
  内容较多时分别成段。
- 三项以上且每项带有条件、解释或后果的并列内容，改用列表或表格。只有名称或很短的同类词可以留在句内。
- 算例按“输入与口径、代入与中间量、结果、能说明什么、不能说明什么”组织。各部分可以合并相邻项，
  但不能把输入、结果和边界全部压进一个超长段落。
- 练习题的题干与提示或答案分开。多步计算使用缩进子项或表格，答案必须仍属于原题编号，不能因空行
  或缩进错误造成编号重置。
- 加粗短语若持续统领多个段落，应评估是否改为真正的小标题；若只是一个段内的引导词，不要为了视觉
  分隔增加会污染目录和 PDF 书签的标题。
- 同时检查“该拆未拆”和“该合未合”。孤立承接句、重复表前说明、连续同义短段应合并；不要形成
  一句一段、每段都带固定标签的碎片化节奏。
- 超过约 5 个完整句子或约 280 个中文字符只作为人工复核信号，不是自动拆段阈值。应先判断该段是否
  跨越多个说明任务，再决定拆分、改列表或保留。

段落修改必须在生成后的网页和 PDF 中抽查。源文件出现空行，只能证明 Markdown 有分段意图，不能证明
最终版式的段间距、列表层级和跨页效果已经清楚。

## 6. 文风规范

本节约束教程正文、README、图中文字和发布产物。代码标识、命令、论文正式方法名、标准名称以及
本文件中的工作流程说明，不按教程正文的口吻处理。

出版文字统一称“本书”或“本教程”，不用“本报告”“这份交付”等编辑身份。`B-1`、`修订块 2`
一类标签只有在正文已经定义、可导航且对读者确有用途时才能保留。中文连续数值范围使用“～”，
不用 ASCII `~`；版本约束、命令、代码和正式标识不受此规则限制。

不要按词表机械替换。判断一句话是否需要修改，先看它有没有准确表达对象、条件、动作和结果。
专业术语比日常说法更准确时，应保留专业术语。

### 6.1 分阶段检查

写或改每一段时，做三项检查：

1. **删除检查**：删掉暖场、标签或收尾句后，事实和因果关系是否不变？不变就删。例如删掉
   “值得注意的是”，直接写后面的结论。
2. **指代检查**：“这、它、前面、这套方法”是否能在本句或上一句找到唯一对象？不能就写出
   具体的信号、变量、模块或指标。
3. **具体化检查**：能否用可观察的动作、条件或数字替换比喻和黑话？例如把“求逆失败时兜底”
   改为“检测到协方差矩阵不可逆时，改用延迟求和”。

改完一个小节后，再检查：

- 同一种段首、提问或收尾模板是否在连续段落或同一小节内反复出现。
- 同一核心概念通常只保留一个主类比；另一个类比只有在解决不同理解困难时才保留。类比后回到
  变量、公式、数字和适用边界。
- “明显、较大、实时、低延迟、通常”等词是否有比较对象、测试条件或量级。无法给出数字时，
  也要说明比较基准和变化方向。
- “必然、完全、天然、只需、任何”等绝对词是否由公式、标准或明确条件支持。
- 并列项各自带有条件或解释，导致句子难以读清时，改用列表；简短的同类词可以留在句内。

准备构建发布产物前，按第 3.1 节确定的范围检查：小修检查改动段及直接上下文，章节级检查目标文件，
全书级才扫描全部发布文本。

- 搜索下列高风险写法并逐条查看上下文。目标不是命中数为零，而是每个保留项都有明确理由。
- 检查是否残留“待补、待核实、新增、本次修改、编号不动”等编辑痕迹。
- 本次任务包含文风清理时，在完工报告中列出已处理的高频模式和有意保留的专业用法。

### 6.2 重点负样本

- **八股连接词**：反复使用“首先、其次、最后、总之”“不是 A，而是 B”“这就是……的本质”。
  有真实步骤就编号写动作；没有顺序关系就删掉连接词。
- **元叙事**：每节都写“人话版、白话说、一句话总结、看什么、记住、承接、幕间小结”。
  直接写事实或过渡关系，不给段落加固定口头标签。
- **工作流话术进入正文**：“本轮、排期、交付、后续迭代、闭环验收”。教程只写读者需要理解或
  执行的内容，编辑过程留在工作记录中。
- **产品和咨询黑话**：“拉通链路、推动落地、赛道红利、能力底座、核心抓手、组合拳、性能天花板、
  方案兜底”。改写为具体模块、指标、限制或失败后的处理动作。
- **伪口语和过度拟人**：“算法把频谱吃进去、把掩码喂给 MVDR、神经网络接管、房间这个猪队友、
  系统翻车或掉链子”。改写为“网络输入语谱图”“用掩码估计协方差”“条件数增大后求逆误差上升”。
- **夸张口语和竞技化标签**：“踩坑、命门、挖出零陷、崩塌、通吃、露馅、发论文、打榜”。直接写
  失效条件、响应变化、研究复现或公开评测目标；专业含义明确的“秩亏”“零陷”不属于此类。
- **营销式断言**：“工业默认、唯一正确、彻底消除、任何场景都适用、直接碾压”。写明数据集、阵列、
  信噪比、模型假设和比较对象；没有证据就降低结论强度。
- **命令读者**：“记死、不许质疑、照抄、默认答案”。改为说明建议、条件和错误后果。
- **不必要的中英混杂**：“refine 协方差、调 trade-off、跑 baseline、pipeline 已 ready”。首次出现
  写“中文名（英文全称，缩写）”，后文使用中文或缩写。

### 6.3 可以保留的用法

- “先、再、最后”用于真实算法步骤时可以保留。步骤较多且行内难以读清时使用编号。
- DOA、STFT、MVDR、WPE 等专业缩写可以保留，但首次出现要给出中文名称和英文全称。
- 代码、命令、API 名称和没有稳定中译名的正式术语保留原文。
- “链路”明确表示信号经过的模块顺序，“上限”由公式或标准给出，“基线”表示可复现实验对照时，
  都是准确用法。
- “不是 A，而是 B”、类比和提问句可以偶尔使用。若删除后不损失信息，或在相邻段落重复，就应改写。

### 6.4 正例

- 过渡：“前一节得到麦间时延。本节用多组时延解算声源位置。”
- 定量结论：“对等权延迟求和波束形成器，若目标信号已正确对齐，且各通道噪声互不相关、方差相等，
  麦克风数量翻倍时白噪声增益增加约 3 dB。”
- 因果关系：“时延估计要做到比采样间隔更细，所以要做插值。”
- 图解：“图中的红色双箭头连接两个 −3 dB 交点，表示主瓣宽度。”
- 类比及边界：“可把对角加载理解为限制权重幅度。这个类比只解释数值稳定性；实际算法是在
  协方差矩阵上增加对角项。”

## 7. 改动与验证

验证范围由改动决定。下表列出最低要求；一项改动同时属于多种类型时，相关验证全部执行。

| 改动类型 | 必须执行的验证 | 通过条件 |
|---|---|---|
| 文字、结构、术语 | 通读改动段及相邻小节；搜索旧术语、占位词和重复模板；检查章节内引用 | 无断句、重复定义和残留旧术语；引用对象存在；结论条件与上下文一致 |
| 公式、推导、手算 | 独立重算；核对量纲、符号、共轭和正负号；检查一个边界或退化情形 | 中间步骤可复算；等式量纲一致；符号与全书约定一致；边界结果符合物理预期 |
| 数值、表格、实验结论 | 用脚本或独立计算复核；记录输入条件和取整方式；核对正文、表格与图 | 在注明精度内复现；比较使用同一口径；各处数字一致 |
| 引用、标准、产品和时效信息 | 查原论文、标准组织或官方页面；核对版本、日期、测试条件和链接 | 来源可访问；正文写明适用版本与条件；关键结论不依赖二手摘要 |
| 绘图脚本 | 重新生成目标图；打开原尺寸图检查坐标、单位、图例、字号、颜色和曲线 | 脚本无错误；随机结果可复现；图与正文一致；没有裁切、遮挡或难辨配色 |
| Markdown 转网页 | 重新构建网站；检查日志、页面数量、内部链接、锚点、图片和公式 | 构建成功；没有缺页、死链、缺图和未渲染公式；导航前后关系正确 |
| 合订 HTML 和 PDF | 重新构建；打开成品，抽查封面、目录、改动页、表格、公式、图片和书签 | 构建成功；改动已进入成品；无溢出、裁切、乱码和空白图；书签指向正确章节 |
| 导航、目录、元数据 | 检查 Markdown、网站和 PDF 三端的标题、顺序、上一篇/下一篇和书签 | 三端名称与顺序一致；所有目标均存在且可到达 |
| 语义交叉引用 | 解析公式号、节号、图号及带具体小节文字的链接；检查声明、引用和链接目标 | 编号唯一且存在；图号与文件名一致；显示为具体小节的链接直达该小节，不只落到章首 |
| 网页与 PDF 可访问性 | 自动检查网页语言、图片替代文本、标题层级、表头、焦点和导航；检查 PDF 文本层、语言、书签、结构标签和阅读顺序，并人工抽查宽表的键盘横向滚动 | 已支持的项目通过；构建链没有结构标签或不能保证阅读顺序时，必须在交付中明确写成限制，不能用“文本可搜索”代替标签化验收 |
| 构建脚本或依赖 | 运行受影响命令及最小回归；检查退出状态和输出文件 | 命令正常结束；输出完整；未引入无关依赖或文件变化 |

当前完整构建的基线是 39 张 PNG、14 个教程页面（首页加 13 篇）与 6 个研究手册页面，以及 PDF 的 14 个章级、119 个节级、
121 个子节级书签，共 254 个大纲项。子节级书签来自第 1～6、8～13 章；各篇独立数量见 `scripts/quality_check.py` 的显式清单。
质量门禁应从独立的发布清单或显式常量读取这些基线，不能只从待检 Markdown 动态生成“期望值”
再自我比较。构建后应核对数量和名称。若本次任务改变章节或图表结构，先更新发布清单、README 和本节，
再按新值验收，不能为了通过检查保留过期数字。

独立门禁还应把源 Markdown 的章节、小节和图片清单与站点、合订 HTML 对照，不能只核对源文件摘要；
网页导航检查应确认每个源 h2/h3/h4 标题片段确实位于当前页 `nav` 中；源 h1 可以排除，正文普通链接不能充数。
摘要一致只能说明生成物对应当前输入，不能证明构建器没有漏掉内容。PDF 文本层、页数和书签可以自动检查，
但 MathJax 异步排版是否完整、宽表或长代码块是否溢出、图片和分页是否裁切，仍须在最终 A4 成品中人工抽查。
Chrome 标签化输出和书签处理后的结构树必须同时保留；存在 `/StructTreeRoot` 只说明有结构标签，
并不等于公式替代文本、辅助技术阅读顺序或 PDF/UA 完整合规已通过，交付时分开报告。
逐页扫描还要标出既无可搜索正文又无图片的页面；章末分隔线、强制分页或不可见元素不能独占一页。
只有图片而无文字的合法页面不应被这个门禁误报，图的可读性仍由人工检查。
还应复核没有图片、却只剩很少正文的近空白页：孤立的章末转场或结尾提示即使含可搜索文字，也可能
被下一章的强制分页挤成单独一页。机器可据字数提示或阻断明显个案，最终需打开相邻两页确认是否
真的孤立；不能仅凭正文不为空就宣称分页通过。
没有可靠自动判据时应明确保留人工验收，不能增加只检查文件大小或固定等待时间的伪门禁。

新增质量检查时，要同时增加“合法输入、非法输入、外形相近但不应命中”的测试夹具。高风险文风词
只能生成带行号的人工复核清单，不能因为词表命中就自动改文或阻断发布；编号重复、目标不存在、图号
与文件名不一致等确定性错误可以阻断发布。

数值算法测试的期望值应来自手算、独立实现、解析边界或外部固定夹具，不能由被测函数本身生成。
实数接口应在类型转换前拒绝意外复数输入，不能以丢弃虚部代替校验。有限输入仍可能在平方、求和、
归一化中溢出；检查中间量，并用安全缩放或明确报错处理。理论上不随整体尺度变化的判据应测试缩放前后
一致性，不能用带隐含单位的绝对阈值替代相对判据。

除典型输入外，还要覆盖公开接口允许的退化情况，例如零杂波权重、极小方差、全缺测、零能量、
秩亏和边界角度；允许参数产生 NaN、除零或静默回退时，必须明确恢复策略并写回归测试。

章节正文发生变化时，网站和 PDF 都要重建。只改内部说明且不进入发布产物时，可以不重建，但要在
完工报告中说明原因。

除表中专项检查外，每次写入后都要检查最终差异，确认没有空白错误、意外改动、残留占位和无关文件。
构建通过不等于内容正确，内容正确也不等于渲染通过，两类检查不能互相替代。

若环境缺少浏览器、依赖或联网条件，不能把对应项目写成“通过”。应记录未执行的验证、缺少的条件和
可复现的后续命令。

## 8. 工程纪律

### 8.1 工作区与现有改动

- 修改前先阅读目标文件，并检查工作区已有变化。
- 用户已有的修改属于用户。不要覆盖、回退或顺手整理与当前任务无关的内容。
- 修改范围发生重叠时，先理解现有差异；无法安全合并再向用户说明。
- 不执行会丢失数据或历史的命令，除非用户明确要求并确认目标范围。
- 大改动先列出提纲和文件边界。不同修改者只处理互不重叠的文件或章节，最后统一检查术语、符号和引用。

### 8.2 源文件与生成物

- 先改源文件，再运行生成流程。不要直接修改可再生的网页、合订 HTML、PDF 或成品图片。
- 插图只通过 `scripts/make_figures.py` 或 `scripts/make_aec_figures.py` 中相应函数生成。随机过程必须固定种子。
- 每张 PNG 应记录生成它的脚本路径和脚本摘要。发布门禁核对摘要，脚本变化而图片未重画时必须失败。
  全量发布还要在当前源文件上重新生成 39 张图；差异必须能追到本次缺陷记录或预期修改。
- 图片验收按网页约 860 px 正文宽度和 A4 约 165 mm 正文宽度检查。PNG 原尺寸清楚不等于嵌入后可读；
  正文、坐标、图例和注释都要在最终尺寸下检查有效字号。
- 网页由 `chapters/` 和构建脚本生成；PDF 由合订 HTML 和 PDF 构建脚本生成。
- 若发现生成物与源文件不一致，先修源文件或构建脚本，再重新生成。

### 8.3 工具、依赖与可复现性

- 使用当前环境提供的文件查看、搜索、编辑和命令执行能力；本文件不依赖某个工具的专有名称。
- 搜索文件和正文时优先使用快速全文搜索；修改前先查看完整上下文。
- 优先使用仓库已有的虚拟环境、依赖和脚本。不要为了方便擅自升级依赖、修改锁文件或引入新框架。
- 仅为核验可选第三方算法而安装依赖时，可使用独立的临时环境，并记录准确版本与源码提交；不要使
  主虚拟环境的依赖集在多人并行工作中悄然变化。临时环境中的成功不自动证明主环境可运行该例。
- 确需新增依赖时，先说明用途、影响和可替代方案；安装后记录版本并完成相关回归。
- 构建环境发生变化时，记录 Python、关键依赖、字体、Chrome 和操作系统版本。随机仿真同时记录种子、
  采样率、信号长度、输入生成方式和归一化口径。
- 正式 PDF 的封面日期使用 `--build-date YYYY-MM-DD` 或 `SOURCE_DATE_EPOCH` 固定；未固定日期的日常构建
  可以用于预览，但不能据此声称产物可逐字节复现。

### 8.4 联网与外部操作

- 公式、标准、产品规格、挑战赛状态和近年研究需要核实时，优先查原论文、标准组织和官方页面。
- 搜索工具用于定位资料，不能替代原文核查。不同实验条件下的数字不能直接横向比较。
- 不在文档、日志或回复中暴露访问令牌、密钥和其他凭据。
- 不得擅自创建外部问题、拉取请求、工单，或发送消息、上传文件、发布网站。只有用户明确要求时才能执行。
- 外部资源不可访问时，记录未核实内容，不得凭记忆补写精确数字。

### 8.5 教学代码、第三方源码与供应链

用户要求“覆盖所有算法”时，先清点本书正文实际定义、推导或直接用于工程选择的算法；用户要求扩充领域研究时，
还要调查能补足现有方法缺陷的相关实现，并在研究文档中解释收录理由。不能只用项目数量宣称覆盖完整。
每个算法在 `codes/COVERAGE.md` 中只能落入一种状态：本仓库可运行
基线、外部参考实现、原理索引或明确排除。不能用占位函数或未经验证的简化程序冒充论文完整实现。

- 本仓库教学实现要尽量少依赖，写明输入输出形状、单位、复数约定、随机种子、公式对应位置和失效边界。
  教学代码用于复算和理解，不默认满足实时、线程安全、定点、内存上限或产品鲁棒性要求。
- 数值测试的期望值来自手算、解析边界或独立实现；被测函数不能生成自己的期望值。核心算法至少覆盖
  一个正常例、一个退化或错误输入，以及一个与正文算例或公式直接对应的检查。
- 正文声称“拒绝、回退、加载、旁路”时，要与代码的实际触发条件逐项对应；内部改用最小二乘等求解
  路径时，若输出可能仍为有限值但物理含义改变，应给调用方可读取的状态或明确的诊断边界。
- 第三方项目只采用作者、标准组织或项目维护者的官方仓库。索引必须记录完整提交哈希或不可变版本、
  仓库地址、核实日期、代码许可证、用途和获取方式；只写浮动分支名不能作为可复现版本。
- 代码、模型权重、训练数据和测试数据的许可证分别核对。代码采用宽松许可证，不代表模型或数据可以
  商用、修改或再分发；论文公开、仓库可访问也不等于获得再分发许可。
- 没有许可证、许可证不清楚、AGPL 等会改变分发义务，或仓库体积较大的项目，默认只做来源索引或按需
  获取，不复制进本仓库。确需纳入源码时，保留 LICENSE、NOTICE、作者信息和必要补丁记录，并先确认
  本仓库自身的发布许可证与之兼容。
- 锁表中的 `fetch_enabled` 只表示本地获取策略，不是代码许可结论。设为 `false` 时在同一条记录写明
  具体原因和解除条件；代码可本地研究而随仓示例音频、权重或数据许可未明时，优先评估能否只取得
  固定提交的源码与许可证。取得后仍分别记录源码核对、依赖构建、示例运行和数值对照，不能把
  `source_verified` 写成算法复现成功。
- 用户要求把可获取源码放到本地时，可以在 Git 忽略的 `codes/upstream/_downloads/` 中建立独立上游工作树，
  保留来源和许可。大型框架、copyleft 许可或权重未授权不自动意味着代码不能本地研究；是否取得代码、是否取得
  权重、是否再分发必须分别判断。不得用缓存忽略规则代替实际下载，也不得把独立下载描述为已并入发布仓库。
- 按需获取脚本必须固定提交并默认只下载、不执行；普通单元测试不得联网。下载压缩包时记录并核对
  SHA-256；使用 Git 时检出完整提交并验证 `HEAD`。含子模块或生成资产时另行固定其来源。
- 已有下载目录核对来源、提交、工作树修改和登记的入口文件；不覆盖不一致目录。每个项目分别记录获取失败、
  源码核对、依赖构建、示例运行和数值对照状态。取得源码或入口文件存在不能充当算法正确性的证据。
- 获取工具不得继承会改变 Git 目标仓库、索引、对象路径或注入配置的环境变量；目录参数不能被外层 Git 状态绕过。
  子集检出要记录实际筛选规则并与清单核对，不能把请求的目录列表直接作为已验证结果。
- 源码筛选规则应写清会省略哪些文件。跳过 LFS、模型和音频扩展名只能减少资产下载，不能证明 Git 对象库或
  源文件内没有模型参数。旧下载目录未按新规则重检时，不得追溯宣称全部符合新策略。
- 外部实现只能支持其自身版本、配置和许可证下的行为。硬件、驱动、GPU、模型权重、数据集或账号是
  额外前提时，文档必须单列，不能写成运行一个命令即可完整复现。
- 对齐外部接口时，不仅比较参数名与数值，还要用带时间标签的小输入检查实际抽头、更新顺序、启动填充和重置。
  因果网络结构不自动证明跨调用状态可续接；评分器返回有限数也不自动证明评分有效，警告、哨兵值与有效区域要单独记录。
- 对上游实现行为进行适配或转写时，明确改编范围并保留所需版权与许可声明，不把改名后的参考程序称为完全独立实现。
  数值期望仍需来自手算、解析边界或另一条独立计算路线；改编程序与上游一致只能证明版本行为一致。
- 新增、替换或删除算法时，同时检查正文、`codes/COVERAGE.md`、`codes/THIRD_PARTY.md`、锁定清单、
  示例和测试。正式交付前检查仓库中没有意外下载的第三方源码、权重、数据、凭据和许可证不明文件。

### 8.6 章节练习与音频样本

- 新增可执行练习使用稳定 ID；正文、答案、示例返回结果、独立测试和 `codes/COVERAGE.md` 一一对应。
  添加练习不等于添加一种算法，不用题目数量增加算法覆盖统计。保留旧题编号，移动题目后按题意检查引用。
- 新增练习应对应明确的学习难点、反例或缺失步骤；不能只换数字重复现有题目来增加数量。
  全书审查报告分别列出已读、已独立复算、已运行和未验证范围，不使用“全部覆盖”代替这些记录。
- 题干、输入口径、计算步骤、答案和适用边界分别清楚呈现；结果不仅给一个数字，还解释它能支持的判断。
  数学结果来自独立手算或独立实现；随机例子固定种子，代码导入不执行实验、写文件或联网。
- 状态型练习固定时间单位、块长、参数更新时刻和状态寿命。算法若应与分块方式无关，比较整段与不同
  分块；若本来依赖分块，解释差异。手算中间量保留足够精度，最后统一舍入，不把近似值写成精确等式。
- 音频生成源在 `codes/array_tutorial/audio_samples.py` 和 `codes/examples/generate_audio_samples.py`。
  不直接修 WAV；改源后重建 `codes/audio/MANIFEST.json` 和受影响图、站点副本。清单记录输入摘要、环境、
  模型与参数、采样率、声道、时长、参考、时延、增益、随机种子、量化与文件摘要。
- GSS 与自由场移动音频各有独立生成脚本、目录和清单。变更后同时核对中间状态或轨迹真值、每个 WAV 的
  帧数/通道/摘要，以及站点副本；不能用主音频清单的 60 个文件数掩盖独立资产缺失。
- 不同音频组采用不同信号模型时，在清单中明确全局模板适用哪些组，其余逐组覆盖参数；不能让
  默认谐波数、包络或淡入淡出说明错误套用于新组。图读取 PCM 时区分解析值与量化后测量值。
- 比较组使用同一显式导出增益，不逐文件峰值归一化；保留参考并说明对齐口径。检查有限性、PCM 格式、
  通道交织、削波和量化误差；预留播放余量不等于能保证用户设备上的安全声压。
- 合成谐波、稀疏回声、已知矩阵解混、真值冻结和双声道声像分别标明其限制，不能称为真实语音、实测房间、
  盲分离、双讲检测或物理 DOA 真值。客观读回和浏览器加载不能替代人工听测或标准主观评价。
- 引入真人录音前核查授权、隐私及再分发条件；数据许可与算法代码许可分别记录。未授权或来源不清楚的录音
  不进入仓库。试听控件默认不自动播放，并提示先调低音量。
- 真实环境录音同样记录官方版本、下载地址、归档和摘录 PCM 摘要、原始通道顺序及半开采样区间。
  派生文件注明裁剪、重采样、增益、均值、量化等改变；署名和数据许可必须随网站音频副本分发。
  只验证录音的通道相关性或输出功率时，不把结果命名为 SNR、语音增强增益或方向准确率；未校准的麦克风
  增益、缺少干净参考与位置真值都需说明。数字域二阶矩是否去均值必须与公式和输出一致。
- `--check` 必须只读，资产或生成源过期时失败，不以重写文件修复检查。严格字节检查受环境影响时明确说明。
  生成中断后的不一致资产不能发布；重新生成、读回及清单校验完成后才重建站点和相关图。

## 9. 完工报告

完工回复按以下顺序写，未发生的项目可以省略：

1. **结果摘要**：用两三句话说明解决了什么，以及是否还有未完成项。
2. **主要改动**：按主题列出，不按操作时间罗列；每项写清改动对象和影响。
3. **文件清单**：列出主要源文件和生成物的可点击路径。
4. **验证结果**：列出实际执行的构建、计算和检查，以及各自结果。不要只写“已测试”。
5. **未验证项与风险**：说明未执行的检查、原因、影响和后续所需条件；没有则省略。
6. **资料来源**：仅在本次修改使用了外部资料时列出，优先给原始来源。

报告必须区分“已通过”“未执行”和“执行失败”。不能因源文件已修改，就声称网页、PDF 或图像已经
验证。不要使用未定义或依赖特定客户端的交付标记；当前客户端支持文件呈现功能时可以使用，否则给出
清晰、可点击的文件路径。

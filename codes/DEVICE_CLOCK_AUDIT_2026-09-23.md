# 真实设备时钟漂移：2026-09-23 环境核查

本记录只说明此次主机是否具备测量两台独立输入设备采样率偏移（SRO）的条件，不是漂移测量结果。没有打开麦克风、录制声音或把现场音频加入仓库。

## 已执行的只读检查

- 在 macOS 主机上运行 `system_profiler SPAudioDataType -json`。提升权限后的设备列表只有一个输入：Apple 内建“MacBook Air麦克风”，1 个输入通道，当前标称 48,000 Hz；另有“MacBook Air扬声器”输出。沙箱内同一命令曾返回空列表，故不以沙箱结果判断硬件数量。
- 用 CoreAudio 的设备属性再次枚举到这两个设备：一个输入、一个输出。两者的 `kAudioDevicePropertyClockDomain` 为相同的非零值。依本机 macOS SDK 的 `AudioHardwareBase.h` 语义，同一非零时钟域表示可硬件同步；不能把这对内建设备当成两个已确认独立的采样时钟。
- [libsamplerate 的已知时钟实验](./research/03_industrial_deployment.md#i04libsamplerate-的有状态重采样)以数学合成输入预设 +100/+150 ppm，验证的是重采样接口和真值补偿，不是设备 SRO 估计。[DEMAND 真实录音](./MDL_REAL_AUDIO_REVIEW.md)的多通道样本来自同一同步采集设备，也不能用于两设备相对时钟测量。

| ID｜位置 | 类别／严重度 | 证据 | 修改建议 | 负责人 | 状态 | 验证结果 |
|---|---|---|---|---|---|---|
| DC-01｜本机 CoreAudio 输入设备 | 实测条件／P2 | 仅见一个输入，且内建输入与输出同属非零时钟域 | 接入第二台独立输入，检查其时钟域和驱动重采样路径后再录制 | 主智能体 | 延期 | 未录音；没有可报告的真实设备 ppm |

## 后续实测所需数据与判据

1. 同时提供两台独立输入设备的型号、标称采样率、时钟域，以及是否经过聚合设备、驱动或应用层隐式重采样。只见不同设备名称还不足以证明时钟独立。
2. 在录音场所确认隐私与许可后，固定两只麦克风的位置，用无语音的共同短宽带事件覆盖至少数分钟；保留两路原始帧序号和单调时间戳。未经授权的现场录音不上传，也不并入本仓库。
3. 在多个时间窗估计同一事件的相对延迟，拟合“延迟—时间”斜率并换算 ppm；同时报告截距、峰值置信度、残差、拟合区间和不确定性。检查残差是否有整样本阶跃，以免把丢样误判为平稳 SRO。
4. 再以独立的事件或时间段核对估计，并明确延迟正负号、设备谁快谁慢。只比较两份 WAV 的总帧数或首尾两个事件，不足以排除启动时差、截断和丢样。

本次结论是“实验前提未满足”，不是“漂移为 0 ppm”，也不是“本机设备没有漂移”。
